#include <string>
#include <vector>
#include <queue>

using namespace std;

int solution(string s) {
    int answer = 0;
    queue<int> q;
    string moveS = "";

    for (int i = 0; i < s.size(); ++i) {
        if (i != 0) {
            moveS = 
        }
    }

    return answer;
}